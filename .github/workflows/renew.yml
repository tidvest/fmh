name: FreeMCHost Renew

on:
  workflow_dispatch:
    inputs:
      headless:
        description: "是否无头模式运行"
        required: false
        default: "true"
  schedule:
    # UTC 时间, 每天 02:00 (北京时间 10:00) 跑一次, 按需调整
    - cron: "0 2 * * *"

permissions:
  actions: write
  contents: read

jobs:
  renew:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install playwright requests
          playwright install --with-deps chromium

      - name: Run renew script
        env:
          FMC_EMAIL: ${{ secrets.FMC_EMAIL }}
          FMC_PASSWORD: ${{ secrets.FMC_PASSWORD }}
          FMC_SERVER_IDS: ${{ secrets.FMC_SERVER_IDS }}
          WXPUSHER_TOKEN: ${{ secrets.WXPUSHER_TOKEN }}
          WXPUSHER_UID: ${{ secrets.WXPUSHER_UID }}
          WXPUSHER_TOPIC_ID: ${{ secrets.WXPUSHER_TOPIC_ID }}
          HEADLESS: ${{ github.event.inputs.headless || 'true' }}
        run: python renew.py

      - name: Upload screenshots (debug)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: debug-artifacts
          path: |
            *.png
          if-no-files-found: ignore

  cleanup-old-runs:
    needs: renew
    if: always()
    runs-on: ubuntu-latest
    permissions:
      actions: write
    steps:
      - name: 只保留最近 2 次运行记录, 其余自动删除
        uses: Mattraks/delete-workflow-runs@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          repository: ${{ github.repository }}
          retain_days: 0
          keep_minimum_runs: 2
